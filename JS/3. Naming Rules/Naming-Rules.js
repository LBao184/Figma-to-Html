// Quy tắc đặt tên biến trong JavaScript:
// 1. Bắt đầu bằng chữ cái (a-z, A-Z), dấu gạch dưới (_) hoặc dấu đô la ($).
// 2. Có thể bao gồm chữ cái, chữ số (0-9), dấu gạch dưới (_) và dấu đô la ($).
// 3. Không được sử dụng từ khóa đã được định nghĩa sẵn trong JavaScript (ví dụ: var, let, const, function, if, else, v.v.).
// 4. Phân biệt chữ hoa và chữ thường (ví dụ: myVariable và MyVariable là hai biến khác nhau).
// 5. Nên sử dụng kiểu đặt tên camelCase cho biến (ví dụ: myVariableName).
// Ví dụ về tên biến hợp lệ:
let myVariable;
let _anotherVariable;
let $dollarVariable;
let userName123;
// Ví dụ về tên biến không hợp lệ:
let 1stVariable;    // Bắt đầu bằng chữ số
let var;            // Sử dụng từ khóa đã định nghĩa sẵn
let my-variable;    // Chứa dấu gạch nối
let my variable;    // Chứa khoảng trắng
// Tóm lại, việc tuân thủ các quy tắc đặt tên biến giúp mã nguồn trở nên rõ ràng, dễ đọc và tránh các lỗi không mong muốn trong quá trình phát triển ứng dụng.